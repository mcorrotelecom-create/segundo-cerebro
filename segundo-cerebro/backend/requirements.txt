fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
pydantic==2.10.3
pydantic-settings==2.7.0
python-multipart==0.0.19
pdfplumber==0.11.4
pikepdf==9.4.2
openpyxl==3.1.5
python-docx==1.1.2
# El modelo de embeddings semántico (sentence-transformers + PyTorch) NO se
# instala por defecto aquí: solo el modelo pesa ~470MB y PyTorch varios
# cientos de MB más — no cabe en el límite de RAM (512MB) del plan gratis
# de Render. Sin él, la aplicación usa automáticamente un respaldo liviano
# (HashingEmbedder, ver app/embeddings.py) — la búsqueda por palabras/texto
# completo funciona igual de bien; lo que se pierde es la búsqueda por
# significado (sinónimos, paráfrasis). Para activar el modelo pesado (ej.
# corriendo en tu PC local, donde sí sobra RAM) instala además
# requirements-semantic.txt — ver README, sección "Búsqueda semántica
# completa (opcional)".
numpy==1.26.4
anthropic==0.40.0
python-dotenv==1.0.1
